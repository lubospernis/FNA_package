network.generate.goh <- function(order,steps,alpha,networks='1',save_id='(empty)',start_date='(empty)',preserve='true',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }