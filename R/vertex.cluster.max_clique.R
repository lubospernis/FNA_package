vertex.cluster.max_clique <- function(id='(all networks)',direction='both',cluster='(empty)',arc_set='(empty)',save_as='max_clique',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }