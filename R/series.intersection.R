series.intersection <- function(network_a='(all networks)',network_b='(all networks)',save_as='intersection',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }