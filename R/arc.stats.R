arc.stats <- function(property,stats,id='(all networks)',missing='Zero',scope='Vertex',direction='undirected',filter='(empty)',save_as='+++[save_as]_[stat]+++',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }