network.generate.core_periphery <- function(core_order,periphery_order,periphery_size,networks='1',save_id='(empty)',start_date='(empty)',preserve='true',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }