network.projection <- function(partition,id='(all networks)',direction='left-to-right',arc_property='(empty)',inherit='false',save_as='projection',preserve='true',algorithm='arc',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }