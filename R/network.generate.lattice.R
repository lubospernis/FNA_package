network.generate.lattice <- function(x_order,y_order,networks='1',direction='both',coordinates='false',save_id='(empty)',start_date='(empty)',preserve='true',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }