arc.stats.cumulative_percentile <- function(property,percentile,id='(all networks)',cluster='(empty)',arc_set='(empty)',save_as='cumulative_percentile',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }