network.generate.neural <- function(input_layer,output_layer,networks='1',hidden_layers='None',coordinates='false',save_id='(empty)',start_date='(empty)',preserve='true',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }