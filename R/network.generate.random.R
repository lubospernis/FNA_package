network.generate.random <- function(order,size,save_id='(empty)',networks='1',directed='true',allow_loops='false',start_date='(empty)',preserve='true',save_to='None',debug='false'){ 
 l <- as.list(match.call()) 
 FNA::exec_command(FNA::check(l))
 }